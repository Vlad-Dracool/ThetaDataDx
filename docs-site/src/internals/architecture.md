# Architecture

## System Overview

ThetaDataDx communicates with three ThetaData services:

| Service | Protocol | Purpose |
|---------|----------|---------|
| **Nexus API** | HTTPS (REST) | Authentication -- exchange email/password for session UUID |
| **MDDS** | gRPC over TLS (port 443) | Historical data -- 60 server-streaming RPCs |
| **FPSS** | Custom binary over TLS/TCP (port 20000/20001) | Real-time streaming -- live quotes, trades, OI |

```
Your Application
    |
    +-- HTTPS POST ---------> Nexus API (nexus-api.thetadata.us)
    |                         Returns session UUID
    |
    +-- gRPC over TLS ------> MDDS (mdds-01.thetadata.us:443)
    |                         60 server-streaming RPCs
    |                         Session UUID in QueryInfo
    |
    +-- TLS/TCP ------------> FPSS (nj-a/nj-b.thetadata.us:20000/20001)
                              Custom binary protocol
                              Email + password auth
                              FIT-encoded ticks
```

## Authentication Flow

1. Client sends `POST /identity/terminal/auth_user` to Nexus API with email, password, and terminal key header
2. Nexus returns a session UUID and user info (including subscription tier)
3. For MDDS: session UUID is embedded in every gRPC request's `QueryInfo.auth_token`
4. For FPSS: email and password are sent directly in a CREDENTIALS frame

The terminal API key (`TD-TERMINAL-KEY` header) is a static UUID that identifies the terminal application, not the user.

## MDDS Protocol (Historical Data)

### Service Definition

- **Package**: `BetaEndpoints`
- **Service**: `BetaThetaTerminal`
- **Methods**: 60 RPCs, all server-streaming (returning `stream ResponseData`)
- **Categories**: Stock, Option, Index, Interest Rate, Calendar

ThetaDataDx wraps all 60 RPCs as 61 `DirectClient` methods (plus 1 convenience range-query variant), generated by the `define_endpoint!` macro.

### Request Structure

Authentication is **in-band** -- the session UUID is inside the protobuf message, not in gRPC metadata headers.

```
Request
  +-- query_info: QueryInfo
  |     +-- auth_token: AuthToken
  |     |     +-- session_uuid (from Nexus)
  |     +-- query_parameters: map
  |     +-- client_type: "rust-thetadatadx"
  |     +-- terminal_git_commit
  |     +-- terminal_version
  +-- params: EndpointSpecificQuery
        +-- symbol, date, interval, etc.
```

### Response Processing Pipeline

```
gRPC stream (multiple ResponseData chunks)
    |
    v
Decompress (zstd, pre-allocated via original_size hint)
    |
    v
Decode protobuf (prost -> DataTable)
    |
    v
Merge chunks (concatenate rows, keep first headers)
    |
    v
Parse to typed ticks (DataTable -> Vec<TradeTick / QuoteTick / ...>)
```

The decompressor uses a slab-recycled thread-local `(Decompressor, Vec<u8>)` pair that retains capacity across calls, avoiding allocator pressure.

### Three Processing Modes

| Mode | Description | Use Case |
|------|-------------|----------|
| `collect_stream` | Materializes all chunks into merged DataTable | Default, most endpoints |
| `for_each_chunk` | Streaming callback per chunk | Custom processing |
| `_stream` variants | Combined gRPC call + chunk processing | Millions of rows (trade history) |

### Concurrent Request Limiting

A semaphore limits in-flight gRPC requests. Default is `2^tier` (from subscription tier), matching the Java terminal. Override via `DirectConfig.mdds_concurrent_requests`.

## FPSS Protocol (Real-Time Streaming)

FPSS is a custom binary protocol over TLS/TCP. See [Wire Protocol](wire-protocol.md) for the full specification.

### Key Design Elements

- **Lock-free dispatch**: Events flow through a disruptor ring buffer (`disruptor-rs` v4), matching Java's LMAX Disruptor pattern
- **Synchronous I/O**: The FPSS reader thread is fully synchronous -- no Tokio on the hot path
- **100ms heartbeat**: Client must send PING frames every 100ms or the server disconnects
- **FIT encoding**: Tick data uses nibble-based variable-length integers with delta compression

### Event Model

Events are split into three categories:
- `FpssData` -- market data (Quote, Trade, OpenInterest, Ohlcvc)
- `FpssControl` -- lifecycle (LoginSuccess, Disconnected, MarketOpen, etc.)
- `RawData` -- unparsed frames

### Reconnection

ThetaDataDx uses **manual reconnection** (deliberate deviation from Java's auto-reconnect). Callers monitor `Disconnected` events and decide when to reconnect.

| Disconnect Reason | Action |
|-------------------|--------|
| Credential/account errors (0,1,2,6,9,17,18) | Permanent -- do NOT reconnect |
| TooManyRequests (12) | Wait 130s, then reconnect |
| All others | Wait 2s, then reconnect |

## Module Architecture

```
thetadatadx crate
  |
  +-- auth/
  |     +-- creds.rs     (creds.txt parser)
  |     +-- nexus.rs     (Nexus HTTP auth)
  |
  +-- codec/
  |     +-- fit.rs       (FIT nibble decoder)
  |     +-- fie.rs       (FIE string encoder)
  |
  +-- fpss/
  |     +-- mod.rs       (FpssClient)
  |     +-- connection.rs (TLS/TCP failover)
  |     +-- framing.rs   (wire frames)
  |     +-- protocol.rs  (contracts, messages)
  |
  +-- types/
  |     +-- enums.rs     (80+ DataType codes)
  |     +-- price.rs     (fixed-point Price)
  |     +-- tick.rs      (Trade/Quote/OHLC/EOD)
  |
  +-- direct.rs          (DirectClient, 61 endpoints via macro)
  +-- config.rs          (DirectConfig)
  +-- decode.rs          (zstd + DataTable)
  +-- greeks.rs          (22 Greeks + IV)
  |
  +-- proto/
        +-- endpoints.proto      (shared types)
        +-- v3_endpoints.proto   (60 server-streaming RPCs)
```

## SDK Architecture

```
              +-------------------+
              |   Your Application |
              +--------+----------+
                       |
        +--------------+--------------+
        |              |              |
   +----v----+   +----v----+   +----v----+
   |  Python |   |   Go   |   |  C++   |
   |  (PyO3) |   |  (CGo) |   | (C API)|
   +---------+   +--------+   +--------+
        |              |              |
        +--------------+--------------+
                       |
              +--------v--------+
              |   C FFI Layer   |
              | thetadatadx-ffi |
              +--------+--------+
                       |
              +--------v--------+
              |   Rust Core     |
              |  thetadatadx    |
              +-----------------+
```

The Python SDK uses PyO3 for direct Rust-to-Python bindings, bypassing the C FFI layer. The Go and C++ SDKs go through the C FFI crate.
